1st ver. solution

class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        i = 0
        for item in nums:
            if item != nums[i]:
                i+=1
                nums[i] = item
        return i+1
